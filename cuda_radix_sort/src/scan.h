#ifndef SCAN_H__
#define SCAN_H__

#include <cmath>

#include "utils.h"

void sum_scan_blelloch(unsigned int* const d_out,
                       const unsigned int* const d_in, const size_t numElems);

#endif
